doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/topic') Server Side JavaScript
    ul
      each temp in topics
        li
          - rid = encodeURIComponent(temp['@rid'])
          a(href='/topic/' + rid)= temp.title
    article
      if topic
        h2= topic.title
        div= topic.description
        div= 'by ' + topic.author
      else
        h2 Welcome
        div This is server side javascript tutorial.
      p
        a(href='/topic/add') add
